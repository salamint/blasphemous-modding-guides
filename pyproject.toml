[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Blasphemous Modding Guides"
authors = [{name = "salamint", email = "salamint@proton.me"}]
dynamic = ["version", "description"]
